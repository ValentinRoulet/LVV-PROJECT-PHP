$(function(){
	$(".chosen-select,.chosen-multiple-select").chosen({
            allow_single_deselect:true,
            no_results_text:'Pas de r&eacute;sultat correspondant à ',
            search_contains: true,
        });	
});